Diagrams in this directory were created by [`draw.io`](https://hub.docker.com/r/fjudith/draw.io/).
`src/` subdirectory contains source files that can be opened with `draw.io` when one needs to modify the diagrams.
